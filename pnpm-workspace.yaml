packages:
  - apps/*
  - packages/*

# esbuild (pulled in via tsx) ships a postinstall build script; approve it so
# pnpm doesn't warn on install.
onlyBuiltDependencies:
  - esbuild

verifyDepsBeforeRun: false

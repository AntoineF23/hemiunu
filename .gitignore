node_modules/
dist/
release/
.turbo/
*.log

# secrets & local data
.env
*.db
*.db-*
.hemiunu/

# Per-project memory the agent writes when run from THIS repo during dev
# (HEMIUNU.md is meant to be committed in real user projects, just not here).
# The global user.md lives in ~/.hemiunu (covered by .hemiunu/ above), and any
# legacy per-user files under context/ are ignored too.
/HEMIUNU.md
context/user.md
context/memory.md

# generated wireframes / prototypes (agent output, not source)
prototypes/

node_modules/
dist/
.turbo/
*.log

# secrets & local data
.env
*.db
*.db-*
.hemiunu/

# per-user memory — each user's own copy, seeded from context/*.md.example on
# first run. The repo ships only the empty templates.
context/user.md
context/memory.md
